[project]
name = "openchoreo-gke-cluster"
version = "1.0.0"
description = "GKE cluster bootstrap for OpenChoreo"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-gcp>=9.0.0,<10.0.0",
    "pulumi-kubernetes>=4.0.0,<5.0.0",
]
